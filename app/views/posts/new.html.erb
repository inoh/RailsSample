<%= form_for(@post) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br />
  <%= f.fields_for @post.author do |a| %>
    <%= a.label :name %>
    <%= a.text_field :name %><br />
  <% end %>
  <%= f.submit %>
<% end %>
